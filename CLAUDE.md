# CLARYEL Claude Code entry / Точка входа Claude Code в CLARYEL

Read [`AGENTS.md`](AGENTS.md) before any analysis, command or change. This public repository is one independently buildable component of the wider CLARYEL architecture. Public contributors follow the local documents listed in `AGENTS.md`; CLARYEL maintainers additionally read the private authoritative context in `claryel-company/claryel-platform`. Always read local `REPOSITORY.yaml` and `NEXT_STEPS.md`.

Перед любым анализом, командой или изменением прочитайте [`AGENTS.md`](AGENTS.md). Этот публичный репозиторий является автономно собираемым компонентом общей архитектуры CLARYEL. Публичные участники соблюдают локальные документы из `AGENTS.md`; сопровождающие CLARYEL дополнительно читают приватный авторитетный контекст в `claryel-company/claryel-platform`. Всегда читайте локальные `REPOSITORY.yaml` и `NEXT_STEPS.md`.

Do not use `/init` to replace the established repository or project rules.

Не используйте `/init` для замены действующих правил репозитория или проекта.
