{
  "name": "@claryel/web-community",
  "version": "0.1.0",
  "private": true,
  "description": "Community Edition of the CLARYEL web platform for building, deploying and managing modern websites.",
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs && node --test",
    "test": "node --test",
    "dev": "npx --yes wrangler@4.112.0 dev",
    "deploy": "npx --yes wrangler@4.112.0 deploy",
    "deploy:dry-run": "npx --yes wrangler@4.112.0 deploy --dry-run"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "BUSL-1.1"
}
